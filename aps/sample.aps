global {
    alias println as System.out.println(x: Object);

    alias printlnAwa as com.github.cao.awa.apsars.ApsGlobal.println(x: Object);
}

final class Sample2 {
    public static xxx() {
        int x = 0;
        int y = 100;
        int z = 123;
        try {
            println("Hello world");
        } catch (ex: IllegalArgumentException or IllegalStateException or IllformedLocaleException) {
            println("Hello exception");
        }
        int n = 111;
    }
}
